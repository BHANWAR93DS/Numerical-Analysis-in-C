# Experiment 1 — Finite Difference Method (First Kind)

Solves `y'' - y = x` with `y(0)=0`, `y(1)=1` using central finite differences and the Thomas algorithm.

Compile: `gcc -std=c11 -Wall -Wextra main.c -o exp01`
