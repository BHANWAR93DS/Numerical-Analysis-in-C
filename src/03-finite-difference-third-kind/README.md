# Experiment 3 — Finite Difference Method (Third Kind)

Solves `y'' - 2y' + y = x` with `y'(0)-y(0)=1` and `y'(1)+y(1)=1` using central finite differences and the Thomas algorithm.

Compile: `gcc -std=c11 -Wall -Wextra main.c -o exp03`
